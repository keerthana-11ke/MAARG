import 'package:flutter/material.dart';

class ShakeSosWrapper extends StatelessWidget {
  final Widget child;

  const ShakeSosWrapper({required this.child, super.key});

  @override
  Widget build(BuildContext context) {
    return child;
  }
}

